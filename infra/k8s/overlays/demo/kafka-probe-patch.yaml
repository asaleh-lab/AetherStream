apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: aether
spec:
  template:
    spec:
      containers:
        - name: kafka
          readinessProbe:
            timeoutSeconds: 10
            periodSeconds: 15
            failureThreshold: 6
