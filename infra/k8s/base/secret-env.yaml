apiVersion: v1
kind: Secret
metadata:
  name: aether-secrets
  namespace: aether
type: Opaque
stringData:
  # Overridden by demo overlay or sealed-secrets in CI.
  AETHER_DB_PASSWORD: aether
  AETHER_DB_URL: jdbc:postgresql://REPLACE_PG_HOST:5432/aetherstream
