apiVersion: v1
kind: Service
metadata:
  name: blazor-dashboard
  namespace: aether
spec:
  type: LoadBalancer
  selector:
    app: blazor-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080
