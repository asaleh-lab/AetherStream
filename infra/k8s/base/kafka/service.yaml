apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: aether
  labels:
    app: kafka
spec:
  selector:
    app: kafka
  ports:
    - name: plaintext
      port: 9092
      targetPort: 9092
