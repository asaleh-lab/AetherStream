apiVersion: 1

providers:
  - name: AetherStream
    orgId: 1
    folder: AetherStream
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
